{
  "shell_cmd": "cmake --build . --target keyshot_network_all && cmake --build . --target keyshot_network_tests && ctest -L network --output-on-failure -j 666",
  "working_dir": "${project_path}",
  "path": "$PATH:/usr/local/bin/:/opt/local/bin/"
}
